//import types from "../actions/album.types";

const initialState = {};

export default function AlbumReducer(state = initialState, action) {
  const { type, payload } = action;

  switch (type) {
    default:
      return state;
  }
}
